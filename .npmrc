# expo-router pulls in a react-dom whose peer range is stricter than the react
# version Expo SDK 54 pins for react-native. Without this, a clean `npm install`
# fails on a peer conflict that does not affect the app at runtime.
legacy-peer-deps=true
